name = "cargo-erp"
compatibility_date = "2026-07-13"
compatibility_flags = ["nodejs_compat"]

# OpenNext output — used when running opennextjs-cloudflare preview/deploy locally
pages_build_output_dir = ".open-next/assets"
